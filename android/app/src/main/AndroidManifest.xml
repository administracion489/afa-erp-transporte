<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|locale|smallestScreenSize|screenLayout|uiMode|navigation|density"
            android:name=".MainActivity"
            android:label="@string/title_activity_main"
            android:theme="@style/AppTheme.NoActionBarLaunch"
            android:launchMode="singleTask"
            android:exported="true">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths"></meta-data>
        </provider>
    </application>

    <!-- Permissions -->

    <uses-permission android:name="android.permission.INTERNET" />

    <!-- Ubicación: requerido para que la app aparezca en Ajustes → Ubicación
         y para que el plugin @capacitor/geolocation muestre el diálogo nativo. -->
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <!-- Rastreo en segundo plano (Waze/pantalla bloqueada). FOREGROUND_SERVICE,
         FOREGROUND_SERVICE_LOCATION y el <service> los aporta el merge del plugin
         @capgo/background-geolocation; aquí solo va el permiso de background. -->
    <uses-permission android:name="android.permission.ACCESS_BACKGROUND_LOCATION" />

    <!-- Notificaciones (Android 13+) -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <!-- Exención de optimización de batería (Doze/App Standby + battery savers del
         fabricante). Caso de uso elegible en Google Play: rastreo de ubicación continuo
         de vehículos durante el servicio. Se solicita con el diálogo del sistema desde
         MainActivity (1 toque), sin cambiar el ciclo del foreground service. -->
    <uses-permission android:name="android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS" />

    <!-- Cámara: escaneo de QR de pasajeros -->
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-feature android:name="android.hardware.camera" android:required="false" />

    <uses-feature android:name="android.hardware.location.gps" android:required="false" tools:replace="android:required" />

    <!-- Deshabilitar el receptor de arranque del plugin background-geolocation.
         En Android 15 / MIUI HyperOS, cuando se instala o actualiza la app este
         receiver intenta iniciar BackgroundGeolocationService inmediatamente (via
         MY_PACKAGE_REPLACED / BOOT_COMPLETED). MIUI bloquea el servicio antes de
         que pueda llamar startForeground() dentro del límite de 5 s → ANR.
         El GPS de segundo plano se inicia solo desde nuestro código JS, que ya
         tiene su propio guard de permisos y el delay de 4 s. -->
    <receiver android:name="com.capgo.capacitor_background_geolocation.GeofenceBootReceiver"
        tools:node="remove" />
</manifest>
