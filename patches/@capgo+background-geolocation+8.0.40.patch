diff --git a/node_modules/@capgo/background-geolocation/android/src/main/java/com/capgo/capacitor_background_geolocation/BackgroundGeolocationService.java b/node_modules/@capgo/background-geolocation/android/src/main/java/com/capgo/capacitor_background_geolocation/BackgroundGeolocationService.java
index 97ff3a8..d380b09 100644
--- a/node_modules/@capgo/background-geolocation/android/src/main/java/com/capgo/capacitor_background_geolocation/BackgroundGeolocationService.java
+++ b/node_modules/@capgo/background-geolocation/android/src/main/java/com/capgo/capacitor_background_geolocation/BackgroundGeolocationService.java
@@ -1,6 +1,8 @@
 package com.capgo.capacitor_background_geolocation;
 
 import android.app.Notification;
+import android.app.NotificationChannel;
+import android.app.NotificationManager;
 import android.app.PendingIntent;
 import android.app.Service;
 import android.content.Context;
@@ -55,6 +57,181 @@ public class BackgroundGeolocationService extends Service {
         return binder;
     }
 
+    // FIX ANR Android 14/15 + MIUI/HyperOS:
+    // startForegroundService() arma un plazo del sistema (~10 s) que EXIGE que el
+    // servicio llame startForeground() cuanto antes, en el hilo principal. El diseño
+    // original del plugin difería startForeground() detrás de un bindService()
+    // asíncrono; con el hilo principal ocupado (el WebView cargando el JS remoto) la
+    // llamada llegaba tarde -> ANR "did not then call Service.startForeground()".
+    // Aquí cumplimos el contrato DE INMEDIATO. LocalBinder.start() vuelve a llamar
+    // startForeground() con el título/mensaje reales (startForeground es idempotente).
+    @Override
+    public int onStartCommand(Intent intent, int flags, int startId) {
+        try {
+            ensureNotificationChannel();
+            Notification notification = createBackgroundNotification("Ubicación activa", "Rastreo en segundo plano");
+            if (Build.VERSION.SDK_INT >= Build.VERSION_CODES.Q) {
+                startForeground(NOTIFICATION_ID, notification, ServiceInfo.FOREGROUND_SERVICE_TYPE_LOCATION);
+            } else {
+                startForeground(NOTIFICATION_ID, notification);
+            }
+        } catch (Exception exception) {
+            Logger.error("onStartCommand: no se pudo iniciar el servicio en primer plano", exception);
+        }
+        // Reset del watchdog por CONTENIDO en cada arranque del servicio (una sesión nueva
+        // no hereda el estado de atasco de la anterior).
+        lastLatClavada = Double.NaN;
+        lastLngClavada = Double.NaN;
+        lastPosChangeMs = 0;
+        nivelAtasco = 0;
+        return START_NOT_STICKY;
+    }
+
+    // Crea el canal de notificación si aún no existe (necesario para startForeground
+    // en onStartCommand, que puede ejecutarse antes de que el plugin cree el canal).
+    private void ensureNotificationChannel() {
+        if (Build.VERSION.SDK_INT >= Build.VERSION_CODES.O) {
+            NotificationManager manager = (NotificationManager) getSystemService(Context.NOTIFICATION_SERVICE);
+            if (manager == null) {
+                return;
+            }
+            String channelId = BackgroundGeolocationService.class.getPackage().getName();
+            if (manager.getNotificationChannel(channelId) == null) {
+                NotificationChannel channel = new NotificationChannel(
+                    channelId,
+                    getAppString("capacitor_background_geolocation_notification_channel_name", "Background Tracking", getApplicationContext()),
+                    NotificationManager.IMPORTANCE_DEFAULT
+                );
+                channel.enableLights(false);
+                channel.enableVibration(false);
+                channel.setSound(null, null);
+                manager.createNotificationChannel(channel);
+            }
+        }
+    }
+
+    // Pide actualizaciones de ubicación de los proveedores disponibles. En celulares con
+    // chip GPS, FUSED/GPS dan la mejor precisión; en tablets WiFi-only SIN chip GPS
+    // (p.ej. Redmi Pad SE) FUSED/NETWORK entregan posición por red/WiFi. El proveedor
+    // que el dispositivo tenga entrega; el resto se ignora sin romper nada. El plugin
+    // original sólo escuchaba GPS_PROVIDER, por eso daba 0 fixes en equipos sin GPS.
+    // Heartbeat por TIEMPO (no por distancia): es el PISO DE CAPTURA. Pide al proveedor
+    // un fix tan seguido como ~2 s, en primer y segundo plano, AUNQUE el vehículo esté
+    // detenido (minDistance=0 evita que GMS bloquee entregas "too close" al estar quieto;
+    // el distanceFilter del JS se ignora a propósito a favor del heartbeat). Captar rápido
+    // NO significa enviar rápido: el lado JS (intervaloEnvioMs en app/conductor) decide el
+    // ritmo REAL de envío de forma ADAPTATIVA (3-5 s en marcha, ~25 s detenido) para no
+    // malgastar batería/datos/costo. Con fixes regulares, además, el watchdog de 60 s nunca
+    // dispara. NOTA batería: 2 s mantiene el chip GPS más activo; subir a 3000-5000 si se
+    // prioriza autonomía (el envío adaptativo seguirá topando en el piso que se elija).
+    private static final long HEARTBEAT_INTERVAL_MS = 2000;
+
+    // ── Watchdog por CONTENIDO (caso #951, 4-jul-2026) ─────────────────────────────────────
+    // El motor FUSED/GMS puede COLGARSE y servir la MISMA posición (lat/lng byte-idénticos) con
+    // timestamps frescos durante todo un servicio — callbacks vivos, watchdog de silencio ciego.
+    // Detección: posición idéntica > POS_CLAVADA_MS con fixes llegando. Escalada silenciosa:
+    //   nivel 1 → re-registrar los proveedores (a veces destraba el pipeline de GMS);
+    //   nivel 2 → añadir GPS_PROVIDER y NETWORK_PROVIDER CRUDOS junto a FUSED (puentea el motor
+    //             fusionado colgado: si el GNSS vivo entrega, cualquier fuente viva gana).
+    // Cualquier cambio de posición resetea el nivel. El aviso al conductor (toggle de Ubicación /
+    // reiniciar el celular) vive en el lado JS; aquí solo intentos de recuperación sin UI.
+    private static final long POS_CLAVADA_MS = 5 * 60_000;
+    private double lastLatClavada = Double.NaN;
+    private double lastLngClavada = Double.NaN;
+    private long lastPosChangeMs = 0;
+    private int nivelAtasco = 0;
+
+    private void chequearPosicionClavada(android.location.Location location) {
+        long ahora = System.currentTimeMillis();
+        double lat = location.getLatitude();
+        double lng = location.getLongitude();
+        if (lat != lastLatClavada || lng != lastLngClavada) {
+            lastLatClavada = lat;
+            lastLngClavada = lng;
+            lastPosChangeMs = ahora;
+            nivelAtasco = 0;
+            return;
+        }
+        if (lastPosChangeMs == 0) {
+            lastPosChangeMs = ahora;
+            return;
+        }
+        if (ahora - lastPosChangeMs < POS_CLAVADA_MS) {
+            return;
+        }
+        if (nivelAtasco == 0) {
+            nivelAtasco = 1;
+            lastPosChangeMs = ahora; // re-armar la ventana antes de volver a escalar
+            Logger.info("Posición clavada >5 min con fixes vivos: re-registrando proveedores (nivel 1)");
+            if (client != null && locationCallback != null) {
+                client.removeUpdates(locationCallback);
+                requestLocationUpdatesMultiProvider(currentDistanceFilter);
+            }
+        } else if (nivelAtasco == 1) {
+            nivelAtasco = 2;
+            lastPosChangeMs = ahora;
+            Logger.info("Posición sigue clavada: añadiendo GPS/NETWORK crudos junto a FUSED (nivel 2)");
+            agregarProveedoresCrudos();
+        }
+        // nivel 2 ya activo: no hay más recuperación posible a nivel app; el JS muestra el banner.
+    }
+
+    // Registra el MISMO listener también en los proveedores crudos (GPS y NETWORK). En API 31+
+    // el registro normal usa solo FUSED; si el motor fusionado está colgado, el GNSS crudo puede
+    // seguir vivo. Registrar el mismo listener en otro proveedor NO duplica el de FUSED (cada
+    // proveedor+listener es un registro independiente; removeUpdates los quita todos).
+    private void agregarProveedoresCrudos() {
+        if (client == null || locationCallback == null) {
+            return;
+        }
+        String[] crudos = { LocationManager.GPS_PROVIDER, LocationManager.NETWORK_PROVIDER };
+        for (String provider : crudos) {
+            try {
+                if (client.isProviderEnabled(provider)) {
+                    client.requestLocationUpdates(provider, HEARTBEAT_INTERVAL_MS, 0f, locationCallback);
+                }
+            } catch (SecurityException ignore) {
+                // Permisos: se gestionan en el lado JS antes de start().
+            } catch (IllegalArgumentException ignore) {
+                // El proveedor no existe en este dispositivo.
+            }
+        }
+    }
+
+    private void requestLocationUpdatesMultiProvider(float distanceFilter) {
+        if (client == null || locationCallback == null) {
+            return;
+        }
+        java.util.List<String> providers = new java.util.ArrayList<>();
+        if (Build.VERSION.SDK_INT >= 31) {
+            // FUSED_PROVIDER (API 31+) fusiona GPS + red + WiFi.
+            providers.add(LocationManager.FUSED_PROVIDER);
+        }
+        providers.add(LocationManager.GPS_PROVIDER);
+        providers.add(LocationManager.NETWORK_PROVIDER);
+
+        boolean requested = false;
+        for (String provider : providers) {
+            try {
+                if (client.isProviderEnabled(provider)) {
+                    client.requestLocationUpdates(provider, HEARTBEAT_INTERVAL_MS, 0f, locationCallback);
+                    requested = true;
+                    // En API 31+ basta FUSED; evita duplicar fixes pidiendo a varios.
+                    if (Build.VERSION.SDK_INT >= 31 && LocationManager.FUSED_PROVIDER.equals(provider)) {
+                        return;
+                    }
+                }
+            } catch (SecurityException ignore) {
+                // Permisos: se gestionan en el lado JS antes de start().
+            } catch (IllegalArgumentException ignore) {
+                // El proveedor no existe en este dispositivo.
+            }
+        }
+        if (!requested) {
+            Logger.info("No hay proveedor de ubicación habilitado (GPS/NETWORK/FUSED)");
+        }
+    }
+
     // Some devices allow a foreground service to outlive the application's main
     // activity, leading to nasty crashes as reported in issue #59. If we learn
     // that the application has been killed, all watchers are stopped and the
@@ -139,11 +316,7 @@ public class BackgroundGeolocationService extends Service {
             if (client == null || locationCallback == null) {
                 return;
             }
-            try {
-                client.requestLocationUpdates(LocationManager.GPS_PROVIDER, 1000, currentDistanceFilter, locationCallback);
-            } catch (SecurityException ignore) {
-                // Permission issues are handled in the start() method
-            }
+            requestLocationUpdatesMultiProvider(currentDistanceFilter);
             startWatchdog();
         };
         watchdogHandler.postDelayed(restartRunnable, 10000);
@@ -168,6 +341,7 @@ public class BackgroundGeolocationService extends Service {
 
     private void handleLocationChanged(android.location.Location location) {
         startWatchdog();
+        chequearPosicionClavada(location);
         if (mediaPlayer != null) {
             double[] point = { location.getLongitude(), location.getLatitude() };
             var offRoute = distancePointToRoute(point) > distanceThreshold;
@@ -213,13 +387,8 @@ public class BackgroundGeolocationService extends Service {
 
             locationCallback = createLocationListener(BackgroundGeolocationService.this);
 
-            try {
-                client.requestLocationUpdates(LocationManager.GPS_PROVIDER, 1000, distanceFilter, locationCallback);
-            } catch (SecurityException ignore) {
-                // According to Android Studio, this method can throw a Security Exception if
-                // permissions are not yet granted. Rather than check the permissions, which is fiddly,
-                // we simply ignore the exception.
-            }
+            // Escuchar TODOS los proveedores disponibles (FUSED/GPS/NETWORK), no sólo GPS.
+            requestLocationUpdatesMultiProvider(distanceFilter);
 
             // Promote the service to the foreground if necessary.
             // Ideally we would only call 'startForeground' if the service is not already
